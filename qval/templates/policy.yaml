# Starter release policy (consumed by `qval gate` once F-06 lands).
# Edit thresholds to match your product's risk tolerance.
release_policy:
  block_on:
    - severity: critical          # any new critical finding blocks release
  pass_rate_floor: 0.90           # minimum overall pass rate
  require_review:
    - severity: high
